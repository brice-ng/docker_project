<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Votre Billet</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: #f4f4f4; padding: 20px; }
        .container { max-width: 600px; margin: auto; background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0px 0px 10px #ccc; }
        h1 { color:rgb(75, 27, 138); }
        .content { margin: 20px 0; }
    </style>
</head>
<body>

<% 
    const dateTime = new Date(reservation.evenement.date_evenement);
    const formattedDate = new Intl.DateTimeFormat("fr-FR", {
        day: "2-digit",
        month: "long",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
        hour12: false
    }).format(dateTime);
    %>


    <div class="container">
        <h1>Votre billet pour <%= reservation.evenement.libelle %></h1>
        <p>Date : <%= formattedDate %></p>

        <div class="content">
            <p>Bonjour <%= billet.nom %>,</p>
            <p>Merci d'avoir réservé avec nous. Vous trouverez votre billet en pièce jointe.</p>

            <p>Scannez le QR Code à l'entrée :</p>
        </div>

        <p>Cordialement,<br>L'équipe de réservation</p>

       
    </div>
</body>
</html>
